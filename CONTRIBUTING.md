# Руководство по внесению вклада (Contributing Guide)

Спасибо за интерес к проекту MikroTik 2FA VPN System! Мы рады вашему вкладу.

## Как внести вклад

### Сообщение об ошибках

Если вы нашли ошибку:
1. Проверьте, не была ли уже зарегистрирована эта ошибка в [Issues](../../issues)
2. Создайте новый Issue с подробным описанием:
   - Версия системы
   - Шаги для воспроизведения
   - Ожидаемое поведение
   - Фактическое поведение
   - Логи (если применимо)

### Предложение новых функций

1. Создайте Issue с описанием предлагаемой функции
2. Объясните, зачем нужна эта функция
3. Опишите, как она должна работать
4. Дождитесь обсуждения перед началом реализации

### Внесение изменений в код

1. **Fork** репозиторий
2. Создайте **ветку** для вашей функции/исправления:
   ```bash
   git checkout -b feature/amazing-feature
   # или
   git checkout -b fix/bug-description
   ```
3. Внесите изменения в код
4. Убедитесь, что код соответствует стандартам:
   - Backend: следуйте PEP 8, используйте type hints
   - Frontend: следуйте ESLint правилам
   - Все новые функции должны иметь документацию
5. **Commit** изменения с понятным сообщением:
   ```bash
   git commit -m "feat: add amazing feature"
   # или
   git commit -m "fix: resolve bug description"
   ```
6. **Push** в вашу ветку:
   ```bash
   git push origin feature/amazing-feature
   ```
7. Создайте **Pull Request**

## Стандарты кода

### Python (Backend)

- Следуйте [PEP 8](https://pep8.org/)
- Используйте type hints
- Документируйте функции и классы (docstrings)
- Минимальная длина строки комментария: 80 символов
- Используйте `black` для форматирования (если настроен)

Пример:
```python
def get_user(user_id: int) -> Optional[User]:
    """
    Получить пользователя по ID.
    
    Args:
        user_id: Идентификатор пользователя
        
    Returns:
        Объект User или None, если не найден
    """
    return db.query(User).filter(User.id == user_id).first()
```

### JavaScript/React (Frontend)

- Следуйте ESLint правилам проекта
- Используйте функциональные компоненты с хуками
- Используйте PropTypes или TypeScript для типизации
- Компоненты должны быть переиспользуемыми

### Git Commit Messages

Используйте [Conventional Commits](https://www.conventionalcommits.org/):

- `feat:` - новая функция
- `fix:` - исправление ошибки
- `docs:` - изменения в документации
- `style:` - форматирование, отсутствующие точки с запятой и т.д.
- `refactor:` - рефакторинг кода
- `test:` - добавление тестов
- `chore:` - обновление задач сборки и т.д.

Примеры:
```
feat: add user registration via Telegram
fix: resolve database connection timeout
docs: update installation guide
refactor: optimize database queries
```

## Локальная разработка

### Настройка окружения

1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/your-username/mikrotik-2fa-vpn.git
   cd mikrotik-2fa-vpn
   ```

2. Создайте виртуальное окружение:
   ```bash
   python3 -m venv venv
   source venv/bin/activate  # Linux/Mac
   # или
   venv\Scripts\activate  # Windows
   ```

3. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

4. Настройте переменные окружения:
   ```bash
   cp .env.example .env
   nano .env  # Отредактируйте необходимые параметры
   ```

5. Для frontend:
   ```bash
   cd frontend
   npm install
   npm run dev
   ```

### Запуск тестов

```bash
# Backend тесты (когда будут добавлены)
pytest

# Frontend тесты (когда будут добавлены)
cd frontend
npm test
```

## Структура проекта

- `backend/` - Backend приложение (FastAPI)
- `frontend/` - Frontend приложение (React)
- `telegram_bot/` - Telegram бот
- `docs/` - Документация
- `scripts/` - Вспомогательные скрипты
- `tests/` - Тесты
- `config/` - Конфигурационные файлы

## Интернационализация

Если вы добавляете новые строки, которые должны отображаться пользователю:
1. Добавьте ключ в `locales/en/messages.json`
2. Добавьте перевод в `locales/ru/messages.json`
3. Используйте функцию `t()` для получения перевода

Подробнее: [docs/i18n.md](docs/i18n.md)

## Вопросы?

Если у вас есть вопросы, создайте Issue или свяжитесь с maintainers проекта.

Спасибо за ваш вклад! 🎉
