# Быстрая инструкция: Настройка SSH для GitHub

## 🚀 Автоматическая настройка (Рекомендуется)

Запустите скрипт, который все сделает автоматически:

```bash
cd /root/mikrotik-2fa-vpn
./setup_ssh_github.sh
```

Скрипт:
1. ✅ Проверит/создаст SSH ключ
2. ✅ Покажет публичный ключ (который нужно добавить на GitHub)
3. ✅ Подождет, пока вы добавите ключ на GitHub
4. ✅ Проверит подключение
5. ✅ Настроит git для использования SSH

## 📋 Ручная настройка (если скрипт не работает)

### Шаг 1: Создайте SSH ключ

```bash
# Создайте SSH ключ (замените email на ваш GitHub email)
ssh-keygen -t ed25519 -C "ваш-email@example.com"

# Или если ed25519 не поддерживается:
# ssh-keygen -t rsa -b 4096 -C "ваш-email@example.com"

# Нажмите Enter для использования пути по умолчанию (~/.ssh/id_ed25519)
# Введите парольную фразу (или нажмите Enter дважды для пустого пароля)
```

### Шаг 2: Покажите публичный ключ

```bash
# Показать публичный ключ
cat ~/.ssh/id_ed25519.pub

# Скопируйте ВСЁ содержимое (начинается с ssh-ed25519 и заканчивается вашим email)
```

### Шаг 3: Добавьте ключ на GitHub

1. Откройте в браузере: https://github.com/settings/keys
2. Нажмите **"New SSH key"** (зеленая кнопка)
3. Заполните форму:
   - **Title**: например `My Server` или `$(hostname)` (любое имя для идентификации)
   - **Key type**: `Authentication Key`
   - **Key**: вставьте скопированный публичный ключ (весь, от начала до конца)
4. Нажмите **"Add SSH key"**

### Шаг 4: Проверьте подключение

```bash
# Проверьте подключение к GitHub
ssh -T git@github.com

# Должно вывести что-то вроде:
# Hi YOUR_USERNAME! You've successfully authenticated, but GitHub does not provide shell access.
```

Если видите сообщение с вашим username - всё готово! ✅

### Шаг 5: Настройте git для использования SSH

```bash
cd /root/mikrotik-2fa-vpn

# Инициализируйте git (если еще не сделано)
git init

# Добавьте файлы
git add .

# Создайте коммит
git commit -m "Initial commit: MikroTik 2FA VPN System"

# Переименуйте ветку в main
git branch -M main

# Добавьте SSH remote (замените YOUR_USERNAME на ваш GitHub логин)
git remote add origin git@github.com:YOUR_USERNAME/mikrotik-2fa-vpn.git

# Проверьте remote
git remote -v

# Загрузите код на GitHub (БЕЗ ПАРОЛЯ!)
git push -u origin main
```

## ✅ Проверка

После `git push` вы должны увидеть что-то вроде:

```
Enumerating objects: ...
Counting objects: ...
Writing objects: ...
To github.com:YOUR_USERNAME/mikrotik-2fa-vpn.git
 * [new branch]      main -> main
Branch 'main' set up to track remote branch 'main' from 'origin'.
```

Если не запрашивается пароль - значит всё работает! 🎉

## ❌ Устранение проблем

### Проблема: "Permission denied (publickey)"

**Причины:**
- Ключ не добавлен на GitHub
- Неправильно скопирован ключ
- Используется неправильный ключ

**Решение:**
```bash
# 1. Проверьте, какой ключ используется
ssh -vT git@github.com

# 2. Убедитесь, что ключ добавлен в ssh-agent
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519

# 3. Проверьте подключение снова
ssh -T git@github.com
```

### Проблема: "Host key verification failed"

**Решение:**
```bash
# Добавьте GitHub в known_hosts
ssh-keyscan github.com >> ~/.ssh/known_hosts

# Или просто примите ключ при первом подключении (введите 'yes')
```

### Проблема: Git все равно просит пароль

**Решение:**
```bash
# Проверьте, какой URL используется для remote
git remote -v

# Если видите https://github.com/... - нужно изменить на SSH:
git remote set-url origin git@github.com:YOUR_USERNAME/mikrotik-2fa-vpn.git

# Проверьте снова
git remote -v
# Должно быть: git@github.com:YOUR_USERNAME/mikrotik-2fa-vpn.git
```

### Проблема: SSH ключ есть, но не работает

**Решение:**
```bash
# 1. Проверьте права доступа к ключам
chmod 700 ~/.ssh
chmod 600 ~/.ssh/id_ed25519
chmod 644 ~/.ssh/id_ed25519.pub

# 2. Проверьте содержимое публичного ключа
cat ~/.ssh/id_ed25519.pub

# 3. Убедитесь, что ключ добавлен на GitHub:
# https://github.com/settings/keys
```

## 📝 Краткая справка

**Показать публичный ключ:**
```bash
cat ~/.ssh/id_ed25519.pub
```

**Проверить подключение:**
```bash
ssh -T git@github.com
```

**Добавить ключ в ssh-agent:**
```bash
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519
```

**Изменить remote на SSH:**
```bash
git remote set-url origin git@github.com:YOUR_USERNAME/mikrotik-2fa-vpn.git
```

**Проверить remote URL:**
```bash
git remote -v
```

---

**Готово! Теперь вы можете публиковать код без пароля!** 🚀
