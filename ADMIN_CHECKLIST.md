# Памятка администратора: Пошаговая настройка системы

## ✅ Чек-лист первоначальной настройки

### Этап 1: Подготовка MikroTik роутера

- [ ] **Включить SSH службу**
  ```
  /ip service enable ssh
  /ip service set ssh port=22
  ```

- [ ] **Создать администратора для системы**
  ```
  /user add name=vpn_system_admin password=SecurePassword123 group=full
  ```

- [ ] **Проверить доступ с сервера системы**
  ```bash
  ssh vpn_system_admin@192.168.1.1
  ```

- [ ] **Создать тестового VPN пользователя**
  
  **Вариант A: User Manager (RouterOS 7+)**
  ```
  /user-manager user add name=vpn_test_user password=TestPass123
  ```
  
  **Вариант B: User Manager (RouterOS 6)**
  ```
  /tool user-manager user add username=vpn_test_user password=TestPass123
  ```
  
  **Вариант C: PPP Secret (если нет User Manager)**
  ```
  /ppp secret add name=vpn_test_user password=TestPass123 service=l2tp
  ```

- [ ] **Проверить, что пользователь создан**
  ```
  /user-manager user print detail
  # или
  /ppp secret print detail
  ```

- [ ] **(Опционально) Создать firewall правило**
  ```
  /ip firewall filter add chain=forward action=accept \
    comment="vpn_test_user" disabled=yes
  ```

### Этап 2: Настройка Telegram бота

- [ ] **Создать бота через @BotFather**
  1. Открыть диалог с [@BotFather](https://t.me/BotFather)
  2. Отправить `/newbot`
  3. Ввести имя бота (например: `MikroTik VPN 2FA Bot`)
  4. Ввести username бота (например: `mikrotik_vpn_2fa_bot`)
  5. **Сохранить токен:** `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`

- [ ] **Настроить описание бота (опционально)**
  ```
  /setdescription
  # Текст: "Бот для двухфакторной аутентификации VPN подключений"
  ```

- [ ] **Настроить команды бота (опционально)**
  ```
  /setcommands
  
  # Список команд:
  start - Главное меню
  register - Регистрация в системе
  request_vpn - Запросить VPN подключение
  my_sessions - Мои активные сессии
  help - Справка
  ```

### Этап 3: Установка системы

- [ ] **Клонировать репозиторий**
  ```bash
  git clone <repository-url> mikrotik-2fa-vpn
  cd mikrotik-2fa-vpn
  ```

- [ ] **Запустить автоматическую установку**
  ```bash
  chmod +x install.sh
  sudo ./install.sh
  ```

- [ ] **Создать .env файл**
  ```bash
  cp .env.example .env
  nano .env
  ```
  
  **Минимальные настройки:**
  ```env
  # Database
  DATABASE_URL=sqlite:///./mikrotik_2fa_vpn.db
  
  # Security
  SECRET_KEY=<сгенерировать случайную строку>
  JWT_SECRET_KEY=<сгенерировать случайную строку>
  
  # Telegram
  TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
  
  # MikroTik (будет настроено через мастер)
  MIKROTIK_HOST=192.168.1.1
  MIKROTIK_PORT=22
  MIKROTIK_USERNAME=vpn_system_admin
  MIKROTIK_PASSWORD=SecurePassword123
  ```

### Этап 4: Первый запуск и создание администратора

- [ ] **Создать первого администратора**
  ```bash
  cd /root/mikrotik-2fa-vpn
  ./scripts/setup_admin.sh
  ```
  
  Ввести данные:
  - Username: `admin`
  - Email: `admin@example.com`
  - Password: `<безопасный пароль>`
  - Full Name: `Главный администратор`

- [ ] **Запустить систему**
  ```bash
  ./scripts/start.sh
  ```
  
  Или через systemd:
  ```bash
  sudo systemctl start mikrotik-2fa-vpn
  sudo systemctl enable mikrotik-2fa-vpn
  ```

- [ ] **Проверить, что сервисы запущены**
  ```bash
  # Проверить backend
  curl http://localhost:8000/health
  
  # Проверить логи
  tail -f /var/log/mikrotik-2fa-vpn/backend.log
  ```

### Этап 5: Настройка через мастер (Setup Wizard)

- [ ] **Открыть веб-интерфейс**
  - Перейти на `http://<IP-сервера>:8000`
  - Войти под созданным администратором

- [ ] **Пройти мастер настройки**
  
  **Шаг 1: Основная информация**
  - Название системы: `MikroTik VPN 2FA`
  - Язык: `Русский`
  - Email администратора: `admin@example.com`
  
  **Шаг 2: Telegram Bot**
  - Telegram Bot Token: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`
  - [Тестировать токен] → должно быть ✅
  
  **Шаг 3: MikroTik**
  - IP/домен: `192.168.1.1`
  - Порт SSH: `22`
  - Имя пользователя: `vpn_system_admin`
  - Пароль: `SecurePassword123`
  - [Тестировать подключение] → должно быть ✅
  
  **Шаг 4: Завершение**
  - [Завершить настройку]

### Этап 6: Проверка подключения к MikroTik

- [ ] **Проверить список пользователей MikroTik**
  - В веб-интерфейсе: `MikroTik → Пользователи`
  - Должен отображаться `vpn_test_user`

- [ ] **Проверить firewall правила (если настроены)**
  - В веб-интерфейсе: `MikroTik → Firewall Rules`

### Этап 7: Тестирование регистрации пользователя

- [ ] **Открыть Telegram бота**
  - Найти бота по username (например: `@mikrotik_vpn_2fa_bot`)
  - Нажать `Start`

- [ ] **Зарегистрироваться**
  - Команда `/register`
  - Ввести ФИО: `Тестовый Пользователь`
  - Получить подтверждение: `Заявка отправлена`

- [ ] **Одобрить регистрацию (веб-интерфейс)**
  - `Запросы на регистрацию`
  - Найти `Тестовый Пользователь`
  - Нажать `Одобрить`

- [ ] **Проверить уведомление в Telegram**
  - Пользователь должен получить: `Ваша заявка одобрена!`

### Этап 8: Привязка MikroTik аккаунта к пользователю

- [ ] **Перейти на страницу "Пользователи"**
  - Найти `Тестовый Пользователь`
  - Нажать `Редактировать`

- [ ] **Привязать MikroTik username**
  - MikroTik аккаунт #1: `vpn_test_user`
  - MikroTik аккаунт #2: _(оставить пустым)_
  - Нажать `Сохранить`

- [ ] **Проверить, что привязка отображается**
  - В таблице пользователей должен быть виден столбец "MikroTik аккаунты"
  - У пользователя должно быть: `vpn_test_user`

### Этап 9: Тестирование VPN подключения

- [ ] **Запросить VPN через Telegram бота**
  - Команда `/request_vpn`
  - Получить сообщение: `✅ Аккаунт активирован: vpn_test_user`

- [ ] **Проверить, что пользователь активирован в MikroTik**
  ```
  # На роутере
  /user-manager user print detail where name=vpn_test_user
  # Должно быть: disabled=no
  ```

- [ ] **Подключиться к VPN**
  - Настроить VPN клиент на компьютере/телефоне
  - Сервер: `<IP роутера>`
  - Логин: `vpn_test_user`
  - Пароль: `TestPass123`
  - Подключиться

- [ ] **Дождаться уведомления 2FA (до 5 минут)**
  - Должно прийти: `❓ Обнаружено подключение к VPN. Это вы?`
  - Кнопки: `[✅ Да, это я]` `[❌ Нет, не я]`

- [ ] **Подтвердить подключение**
  - Нажать `✅ Да, это я`
  - Получить: `✅ VPN подключение активировано!`

- [ ] **Проверить активную сессию**
  - В веб-интерфейсе: `VPN Сессии`
  - Должна быть сессия со статусом `active`
  - Команда в боте: `/my_sessions`

### Этап 10: Тестирование напоминаний

- [ ] **Дождаться напоминания (по умолчанию через 6 часов)**
  - Для теста можно изменить в настройках на 5 минут
  - Должно прийти: `⏰ Напоминание о VPN. Продолжить работу?`

- [ ] **Продлить сессию**
  - Нажать `✅ Да, продолжить`
  - Получить: `✅ Сессия продлена`

- [ ] **Или отключиться**
  - Нажать `❌ Нет, отключить`
  - Получить: `❌ VPN отключен`
  - VPN соединение должно разорваться

---

## 📋 Ежедневные задачи администратора

### Проверка системы

- [ ] Проверить логи на ошибки
  ```bash
  tail -f /var/log/mikrotik-2fa-vpn/backend.log
  tail -f /var/log/mikrotik-2fa-vpn/telegram-bot.log
  ```

- [ ] Проверить активные сессии
  - Веб-интерфейс: `Дашборд` или `VPN Сессии`

- [ ] Проверить новые запросы на регистрацию
  - Веб-интерфейс: `Запросы на регистрацию`

### Регулярное обслуживание

- [ ] **Ежедневно:** Резервное копирование базы данных
  ```bash
  ./scripts/backup_db.sh
  ```

- [ ] **Еженедельно:** Проверка обновлений
  ```bash
  git pull origin main
  ./scripts/update.sh
  ```

- [ ] **Ежемесячно:** Очистка старых сессий и логов
  - Веб-интерфейс: `Настройки → База данных → Оптимизация`

---

## 🆘 Решение проблем

### Проблема: Telegram бот не отвечает

**Проверить:**
```bash
# Запущен ли бот?
ps aux | grep telegram_bot

# Логи бота
tail -f /var/log/mikrotik-2fa-vpn/telegram-bot.log

# Перезапустить бота
sudo systemctl restart mikrotik-2fa-vpn-telegram-bot
```

### Проблема: Не удается подключиться к MikroTik

**Проверить:**
```bash
# Доступен ли роутер?
ping 192.168.1.1

# Открыт ли порт SSH?
telnet 192.168.1.1 22

# Правильные ли учетные данные?
ssh vpn_system_admin@192.168.1.1
```

### Проблема: Система не обнаруживает VPN подключения

**Проверить:**
```bash
# Запущен ли планировщик?
tail -f /var/log/mikrotik-2fa-vpn/backend.log | grep "check_vpn_connections"

# Есть ли активные подключения на роутере?
ssh vpn_system_admin@192.168.1.1
/user-manager active print detail
# или
/ppp active print detail
```

### Проблема: Пользователь не получает уведомления

**Проверить:**
- Привязан ли Telegram ID к пользователю?
  - Веб-интерфейс: `Пользователи` → проверить столбец "Telegram ID"
- Заблокировал ли пользователь бота?
  - Попросить пользователя разблокировать и отправить `/start`

---

## 📞 Контакты для помощи

- **Документация:** [SYSTEM_WORKFLOW.md](SYSTEM_WORKFLOW.md)
- **Диаграммы:** [docs/system_flow_diagram.md](docs/system_flow_diagram.md)
- **Логи:** `/var/log/mikrotik-2fa-vpn/`
- **Конфигурация:** `.env`

---

**Версия:** 1.0  
**Дата:** 2026-01-10
