# Быстрый старт

## 🚀 Установка и запуск (1 команда)

```bash
wget -O install.sh "https://raw.githubusercontent.com/netcore-24/2FA-Mikrotik-VPN/main/install.sh"
sudo bash install.sh
```

Скрипт автоматически развернёт систему, запустит веб и выведет в терминале **ссылку вида** `http://<IP>:8000`.

## 🧭 Дальше — настройка в веб-интерфейсе

- Откройте ссылку из терминала
- Войдите администратором (логин/пароль выводятся в терминал; также могут быть сохранены в `.admin_credentials.txt`)
- Пройдите мастер настройки (Telegram / MikroTik / параметры)

## 🛠 Управление сервисом

```bash
sudo systemctl status mikrotik-2fa-vpn
sudo systemctl restart mikrotik-2fa-vpn
sudo journalctl -u mikrotik-2fa-vpn.service -f
```

## 🤖 Telegram бот (опционально)

Токен задаётся через мастер настройки в веб-интерфейсе.

