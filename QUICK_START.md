# Быстрый старт - Развертывание на сервере

## ✅ Проект успешно развернут!

### Что уже готово:

1. ✅ Все зависимости установлены
2. ✅ База данных инициализирована
3. ✅ Frontend собран
4. ✅ Администратор создан
5. ✅ Конфигурация настроена

## 🚀 Запуск приложения

### Вариант 1: Запуск через скрипт (рекомендуется)

```bash
cd /root/mikrotik-2fa-vpn
./scripts/start.sh
```

### Вариант 2: Запуск напрямую

```bash
cd /root/mikrotik-2fa-vpn
source venv/bin/activate
uvicorn backend.main:app --host 0.0.0.0 --port 8000
```

### Вариант 3: Запуск как systemd service

```bash
# Копируем service файл
sudo cp /root/mikrotik-2fa-vpn/mikrotik-2fa-vpn.service /etc/systemd/system/

# Редактируем пути в файле (если нужно)
sudo nano /etc/systemd/system/mikrotik-2fa-vpn.service

# Перезагружаем systemd
sudo systemctl daemon-reload

# Запускаем службу
sudo systemctl start mikrotik-2fa-vpn
sudo systemctl enable mikrotik-2fa-vpn

# Проверяем статус
sudo systemctl status mikrotik-2fa-vpn
```

## 🔐 Доступ к системе

После запуска backend откройте в браузере:

- **Веб-интерфейс:** http://localhost:8000
- **API документация:** http://localhost:8000/docs
- **API альтернативная документация:** http://localhost:8000/redoc

**Учетные данные администратора:**
- Username: `admin`
- Password: `admin123`

⚠️ **ВАЖНО:** Смените пароль после первого входа!

## 🤖 Запуск Telegram бота

```bash
cd /root/mikrotik-2fa-vpn
./scripts/start_bot.sh
```

**Перед запуском бота:**
1. Получите токен у @BotFather в Telegram
2. Добавьте в `.env` файл:
   ```
   TELEGRAM_BOT_TOKEN=your_token_here
   ```

## ⚙️ Настройка

### ✅ Вся настройка через веб-интерфейс!

**ВАЖНО:** Все параметры настраиваются через веб-интерфейс в мастер настройки. Ручное редактирование `.env` файла НЕ требуется!

### Шаги настройки:

1. **Войдите в систему** через веб-интерфейс: http://localhost:8000

2. **Откройте "Мастер настройки"** из меню (или он откроется автоматически при первом входе)

3. **Пройдите шаги мастера:**
   - Основная информация (название, язык)
   - Безопасность (первый администратор)
   - **Telegram Bot** - вставьте токен от @BotFather, нажмите "Тестировать"
   - **MikroTik Router** - укажите параметры подключения, протестируйте
   - Уведомления (опционально)
   - Проверка и завершение

4. **Все настройки сохраняются автоматически:**
   - В базу данных
   - Синхронизируются с `.env` файлом
   - Применяются без перезапуска (или требуется перезапуск сервиса)

### Альтернативный способ (только для продвинутых пользователей):

Если вы хотите настроить вручную через `.env`, отредактируйте файл и перезапустите сервис:
```bash
sudo nano /opt/mikrotik-2fa-vpn/.env
sudo systemctl restart mikrotik-2fa-vpn
```

**Но рекомендуется использовать мастер настройки через веб-интерфейс!**

## 📊 Проверка работоспособности

### Проверка Backend:

```bash
curl http://localhost:8000/health
```

Должен вернуть:
```json
{"status": "ok", "version": "1.0.0"}
```

### Проверка API:

```bash
curl http://localhost:8000/api/auth/login \
  -X POST \
  -H "Content-Type: application/json" \
  -d '{"username": "admin", "password": "admin123"}'
```

Должен вернуть JWT токены.

### Проверка Frontend:

Откройте в браузере: http://localhost:8000

Должна открыться страница входа.

## 🔧 Полезные команды

### Создание нового администратора:

```bash
cd /root/mikrotik-2fa-vpn
./scripts/setup_admin.sh
```

### Резервное копирование БД:

```bash
cd /root/mikrotik-2fa-vpn
./scripts/backup_db.sh
```

### Обновление проекта:

```bash
cd /root/mikrotik-2fa-vpn
./scripts/update.sh
```

### Пересборка Frontend:

```bash
cd /root/mikrotik-2fa-vpn/frontend
npm run build
```

## 📝 Структура проекта

```
/root/mikrotik-2fa-vpn/
├── backend/          # Backend приложение (FastAPI)
├── frontend/         # Frontend приложение (React)
├── telegram_bot/     # Telegram бот
├── config/           # Конфигурация
├── scripts/          # Скрипты управления
├── data/             # Данные (БД, бэкапы)
├── logs/             # Логи
├── venv/             # Виртуальное окружение Python
└── .env              # Переменные окружения
```

## 🐛 Решение проблем

### Проблема: Порт 8000 занят

```bash
# Найти процесс
lsof -i :8000

# Остановить процесс
kill -9 <PID>

# Или использовать другой порт
uvicorn backend.main:app --host 0.0.0.0 --port 8001
```

### Проблема: База данных заблокирована

```bash
# Проверить блокировки
fuser data/database.db

# Убить процессы использующие БД
fuser -k data/database.db
```

### Проблема: Frontend не отображается

1. Убедитесь что frontend собран: `ls -la frontend/dist/`
2. Пересоберите: `cd frontend && npm run build`
3. Проверьте что backend правильно настроен для обслуживания статики

## 📚 Дополнительная документация

- [INSTALL.md](INSTALL.md) - Подробная инструкция по установке
- [README.md](README.md) - Общая информация о проекте
- [DEPLOYMENT_STATUS.md](DEPLOYMENT_STATUS.md) - Статус развертывания
- [ENHANCEMENTS.md](ENHANCEMENTS.md) - Дополнения и улучшения

---

**Проект готов к использованию!** 🎉
