# Настройка аутентификации GitHub

## ⚠️ Важно: GitHub больше не принимает пароли!

С августа 2021 года GitHub **не принимает обычные пароли** для git операций через командную строку. Нужно использовать **Personal Access Token (PAT)** или **SSH ключи**.

## 🔑 Способ 1: Personal Access Token (PAT) - Рекомендуется

### Шаг 1: Создайте Personal Access Token на GitHub

1. Войдите на GitHub через веб-браузер
2. Перейдите: **Settings** (нажмите на ваш аватар в правом верхнем углу) → **Developer settings** (внизу слева)
3. Нажмите **Personal access tokens** → **Tokens (classic)**
4. Нажмите **Generate new token** → **Generate new token (classic)**
5. Заполните форму:
   - **Note**: `MikroTik 2FA VPN Project` (любое имя для вашего токена)
   - **Expiration**: Выберите срок действия (90 дней, 1 год, или No expiration)
   - **Select scopes**: Отметьте **`repo`** (это даст полный доступ к репозиториям)
     - ✅ repo
       - ✅ repo:status
       - ✅ repo_deployment
       - ✅ public_repo
       - ✅ repo:invite
       - ✅ security_events
6. Прокрутите вниз и нажмите **Generate token**
7. ⚠️ **ВАЖНО**: Скопируйте токен сразу! Он будет показан только один раз!
   - Токен будет выглядеть примерно так: `ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`

### Шаг 2: Используйте токен вместо пароля

Когда будете выполнять `git push`, используйте:
- **Username**: ваш GitHub логин
- **Password**: ваш Personal Access Token (тот, что вы скопировали)

```bash
cd /root/mikrotik-2fa-vpn

# Инициализируйте git (если еще не сделано)
git init

# Добавьте файлы
git add .

# Создайте коммит
git commit -m "Initial commit: MikroTik 2FA VPN System"

# Переименуйте ветку
git branch -M main

# Добавьте remote (замените YOUR_USERNAME на ваш GitHub логин)
git remote add origin https://github.com/YOUR_USERNAME/mikrotik-2fa-vpn.git

# Загрузите код (при запросе пароля введите ваш токен)
git push -u origin main
```

Когда git попросит:
- **Username for 'https://github.com'**: ваш GitHub логин
- **Password for 'https://YOUR_USERNAME@github.com'**: вставьте ваш Personal Access Token (не пароль!)

### Шаг 3: Сохраните токен для будущего использования (опционально)

Git может сохранить токен, чтобы не вводить его каждый раз:

```bash
# Настройте git credential helper
git config --global credential.helper store

# Теперь при первом push токен будет сохранен
git push -u origin main
```

⚠️ **Безопасность**: Если вы используете `store`, токен будет сохранен в открытом виде в `~/.git-credentials`. Более безопасный вариант - использовать `cache`:

```bash
# Кеширует токен на 1 час
git config --global credential.helper 'cache --timeout=3600'

# Или на 1 день (86400 секунд)
git config --global credential.helper 'cache --timeout=86400'
```

## 🔐 Способ 2: SSH ключи (Альтернатива)

Если вы предпочитаете SSH вместо HTTPS:

### Шаг 1: Создайте SSH ключ

```bash
# Сгенерируйте SSH ключ (замените email на ваш GitHub email)
ssh-keygen -t ed25519 -C "ваш-email@example.com"

# Или если ed25519 не поддерживается, используйте RSA:
# ssh-keygen -t rsa -b 4096 -C "ваш-email@example.com"

# Нажмите Enter для использования пути по умолчанию (~/.ssh/id_ed25519)
# Введите парольную фразу (или оставьте пустым и нажмите Enter дважды)
```

### Шаг 2: Добавьте SSH ключ в GitHub

```bash
# Показать публичный ключ
cat ~/.ssh/id_ed25519.pub

# Скопируйте весь вывод (начинается с ssh-ed25519 и заканчивается вашим email)
```

1. На GitHub: **Settings** → **SSH and GPG keys**
2. Нажмите **New SSH key**
3. **Title**: `My Server` (любое имя)
4. **Key**: Вставьте скопированный публичный ключ
5. Нажмите **Add SSH key**

### Шаг 3: Используйте SSH URL вместо HTTPS

```bash
# Удалите HTTPS remote (если был добавлен)
git remote remove origin

# Добавьте SSH remote (замените YOUR_USERNAME на ваш GitHub логин)
git remote add origin git@github.com:YOUR_USERNAME/mikrotik-2fa-vpn.git

# Загрузите код (без пароля!)
git push -u origin main
```

### Проверка SSH подключения

```bash
# Проверьте подключение к GitHub через SSH
ssh -T git@github.com

# Должно вывести: Hi YOUR_USERNAME! You've successfully authenticated, but GitHub does not provide shell access.
```

## 🛠️ Устранение проблем

### Проблема: "fatal: Authentication failed"

**Решение:**
- Убедитесь, что используете Personal Access Token, а не пароль
- Проверьте, что токен имеет права `repo`
- Проверьте, что токен не истек

### Проблема: Git все равно просит пароль

**Решение:**
```bash
# Очистите сохраненные учетные данные
git credential reject https://github.com

# Или удалите файл с учетными данными
rm ~/.git-credentials

# Попробуйте снова с новым токеном
git push
```

### Проблема: "Permission denied (publickey)" при SSH

**Решение:**
```bash
# Проверьте, что SSH агент запущен
eval "$(ssh-agent -s)"

# Добавьте ваш SSH ключ
ssh-add ~/.ssh/id_ed25519

# Проверьте подключение
ssh -T git@github.com
```

### Проблема: "remote: Support for password authentication was removed"

**Решение:** Это означает, что вы используете обычный пароль. Используйте Personal Access Token (см. Способ 1 выше).

## 📝 Краткая инструкция (Quick Start)

### Если вы хотите быстро опубликовать проект:

1. **Создайте токен**: GitHub → Settings → Developer settings → Personal access tokens → Generate new token (classic) → Выберите `repo` → Generate

2. **Скопируйте токен** (выглядит как `ghp_xxxxxxxx...`)

3. **Выполните команды:**
```bash
cd /root/mikrotik-2fa-vpn
git init
git add .
git commit -m "Initial commit: MikroTik 2FA VPN System"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/mikrotik-2fa-vpn.git
git push -u origin main
# Username: ваш_github_логин
# Password: ваш_персональный_токен (не пароль!)
```

## ✅ Рекомендация

**Для начинающих**: Используйте **Personal Access Token (PAT)** - это проще и быстрее настроить.

**Для опытных пользователей**: Используйте **SSH ключи** - более безопасно и удобно в долгосрочной перспективе.

## 🔒 Безопасность

- ⚠️ **НИКОГДА** не публикуйте ваш Personal Access Token в коде или на GitHub!
- ⚠️ Если токен случайно попал в публичный репозиторий - **немедленно удалите его** и создайте новый
- ⚠️ Регулярно обновляйте токены (раз в несколько месяцев)
- ⚠️ Используйте минимально необходимые права доступа

---

**Теперь вы можете безопасно публиковать ваш проект на GitHub!** 🚀
