# Инструкция по публикации проекта на GitHub

Этот документ содержит пошаговую инструкцию по добавлению проекта на GitHub.

## Подготовка проекта

### 1. Проверка файлов

Убедитесь, что следующие файлы присутствуют и правильно настроены:

✅ **Уже настроено:**
- `.gitignore` - исключает секреты, БД, логи, node_modules и т.д.
- `README.md` - основная документация проекта
- `.env.example` - пример конфигурации (без секретов)
- `LICENSE` - лицензия проекта (MIT)
- `CONTRIBUTING.md` - руководство для контрибьюторов

### 2. Проверка, что секреты не попали в репозиторий

Проверьте, что следующие файлы/данные **НЕ** будут закоммичены:

- ❌ `.env` файлы (уже в `.gitignore`)
- ❌ База данных `*.db` файлы (уже в `.gitignore`)
- ❌ Логи `*.log` (уже в `.gitignore`)
- ❌ Секретные ключи, пароли, токены
- ❌ `node_modules/` (уже в `.gitignore`)
- ❌ `__pycache__/` (уже в `.gitignore`)
- ❌ Ключи SSH (`*.key`, `*.pem`, `*.crt` - уже в `.gitignore`)

### 3. Проверка git статуса

Перед первым коммитом проверьте, что будет добавлено:

```bash
cd /root/mikrotik-2fa-vpn
git status
```

Убедитесь, что в списке **нет** файлов с секретами!

## Создание репозитория на GitHub

### Шаг 1: Создайте новый репозиторий на GitHub

1. Перейдите на [github.com](https://github.com)
2. Нажмите **"+"** → **"New repository"**
3. Заполните:
   - **Repository name**: `mikrotik-2fa-vpn`
   - **Description**: `Система двухфакторной аутентификации для VPN подключений на роутерах MikroTik`
   - **Visibility**: Public (или Private, если хотите)
   - ⚠️ **НЕ** ставьте галочки "Add a README", "Add .gitignore", "Choose a license" (у нас уже есть)
4. Нажмите **"Create repository"**

### Шаг 2: Инициализация git (если еще не сделано)

Если git еще не инициализирован в проекте:

```bash
cd /root/mikrotik-2fa-vpn
git init
```

### Шаг 3: Добавление файлов и первый коммит

```bash
# Проверьте статус перед коммитом
git status

# Добавьте все файлы
git add .

# Проверьте еще раз, что нет секретов
git status

# Создайте первый коммит
git commit -m "Initial commit: MikroTik 2FA VPN System

- Full backend with FastAPI
- React frontend with all pages
- Telegram bot integration
- MikroTik router integration
- Complete documentation
- Automated installation scripts"

# Переименуйте ветку в main (если нужно)
git branch -M main
```

### Шаг 4: Добавление remote и push

Замените `YOUR_USERNAME` на ваш GitHub username:

```bash
# Добавьте remote репозиторий
git remote add origin https://github.com/YOUR_USERNAME/mikrotik-2fa-vpn.git

# Или если используете SSH:
# git remote add origin git@github.com:YOUR_USERNAME/mikrotik-2fa-vpn.git

# Проверьте remote
git remote -v

# Запушьте код
git push -u origin main
```

Если возникнут проблемы с аутентификацией, используйте [Personal Access Token](https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/creating-a-personal-access-token) вместо пароля.

## Дополнительные настройки GitHub

### 1. Добавление описания и тегов

На странице репозитория на GitHub:
- Добавьте **Topics** (теги): `mikrotik`, `vpn`, `2fa`, `telegram-bot`, `fastapi`, `react`, `python`
- Добавьте **Website** (если есть): ссылка на документацию или демо

### 2. Настройка защиты главной ветки (опционально)

Settings → Branches → Add rule:
- Require pull request reviews before merging
- Require status checks to pass before merging

### 3. GitHub Actions (CI/CD) - опционально

Можно добавить автоматическую проверку кода. Создайте файл `.github/workflows/ci.yml`:

```yaml
name: CI

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
      - name: Run linter
        run: |
          pip install flake8 black
          flake8 backend/ --max-line-length=120
          black --check backend/
```

### 4. GitHub Pages для документации (опционально)

Если хотите опубликовать документацию:
- Settings → Pages
- Source: Deploy from a branch
- Branch: `main` → `/docs`

## Обновление проекта

После внесения изменений:

```bash
git add .
git commit -m "Описание изменений"
git push
```

## Полезные ссылки

- [GitHub Docs](https://docs.github.com/)
- [Git Handbook](https://guides.github.com/introduction/git-handbook/)
- [GitHub Flow](https://guides.github.com/introduction/flow/)

## Частые проблемы

### Проблема: "fatal: refusing to merge unrelated histories"

Решение:
```bash
git pull origin main --allow-unrelated-histories
```

### Проблема: "Permission denied (publickey)"

Решение: Настройте SSH ключи или используйте HTTPS с Personal Access Token

### Проблема: Секреты попали в историю коммитов

Решение: Используйте [git-secret](https://git-secret.io/) или пересоздайте репозиторий (если еще не опубликован)

## Готово! 🎉

Ваш проект теперь на GitHub! Не забудьте:
- ✅ Обновить ссылки в README.md (если там есть хардкод ссылок)
- ✅ Создать первый Release (опционально)
- ✅ Добавить badges в README (опционально)

---

**Важно**: Никогда не коммитьте файлы с секретами! Всегда проверяйте `git status` перед коммитом.
