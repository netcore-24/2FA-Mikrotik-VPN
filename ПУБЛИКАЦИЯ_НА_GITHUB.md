# Инструкция по публикации проекта на GitHub

## ✅ Что уже готово

Все необходимые файлы для публикации на GitHub уже созданы:

1. ✅ **`.gitignore`** - правильно настроен, исключает все секреты:
   - `.env` файлы
   - Базы данных (`*.db`)
   - Логи (`*.log`)
   - `node_modules/`
   - `__pycache__/`
   - SSH ключи и сертификаты
   - IDE файлы

2. ✅ **`README.md`** - полная документация проекта на русском языке

3. ✅ **`LICENSE`** - лицензия MIT для открытого исходного кода

4. ✅ **`CONTRIBUTING.md`** - руководство для контрибьюторов

5. ✅ **`GITHUB_SETUP.md`** - подробная инструкция на английском

6. ✅ **`.env.example`** - пример конфигурации без секретов

7. ✅ **Проверка безопасности** - нет файлов с секретами в репозитории

## 🚀 Автоматическая настройка (Рекомендуется)

### Вариант 1: SSH (Рекомендуется, если можете добавить SSH ключ на GitHub)

```bash
cd /root/mikrotik-2fa-vpn
./setup_ssh_github.sh
```

Скрипт автоматически:
- ✅ Проверит/создаст SSH ключ
- ✅ Покажет публичный ключ для добавления на GitHub
- ✅ Проверит подключение к GitHub
- ✅ Настроит git для использования SSH
- ✅ Инициализирует репозиторий и создаст первый коммит

**Подробная инструкция**: см. файл `SSH_ИНСТРУКЦИЯ.md`

### Вариант 2: Personal Access Token (если SSH не подходит)

```bash
cd /root/mikrotik-2fa-vpn
./setup_github.sh
```

**После скрипта**: следуйте инструкциям на экране для создания Personal Access Token.  
**Подробная инструкция**: см. файл `GITHUB_AUTH.md`

## 📋 Ручная настройка

### Шаг 1: Установите Git (если не установлен)

```bash
# Debian/Ubuntu
sudo apt update
sudo apt install git -y

# Проверьте установку
git --version
```

### Шаг 2: Настройте Git (если еще не настроен)

```bash
git config --global user.name "Ваше Имя"
git config --global user.email "ваш-email@example.com"
```

### Шаг 3: Создайте репозиторий на GitHub

1. Перейдите на [github.com](https://github.com)
2. Нажмите **"+"** → **"New repository"**
3. Название: `mikrotik-2fa-vpn`
4. Описание: `Система двухфакторной аутентификации для VPN подключений на роутерах MikroTik`
5. **Public** или **Private** (на ваш выбор)
6. ⚠️ **НЕ** ставьте галочки "Add a README", "Add .gitignore", "Choose a license" (у нас уже есть!)
7. Нажмите **"Create repository"**

### Шаг 4: Инициализируйте Git и загрузите код

```bash
cd /root/mikrotik-2fa-vpn

# Инициализируйте git (если еще не сделано)
git init

# Проверьте, что нет секретов в файлах
git status

# Добавьте все файлы
git add .

# Проверьте еще раз список файлов
git status

# Создайте первый коммит
git commit -m "Initial commit: MikroTik 2FA VPN System

- Full backend with FastAPI (60+ API endpoints)
- React frontend with 9 complete pages
- Telegram bot integration
- MikroTik router integration (SSH + REST API)
- Complete documentation
- Automated installation scripts
- Internationalization (ru/en)
- Audit logs and statistics"

# Переименуйте ветку в main
git branch -M main

# Добавьте remote репозиторий (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/mikrotik-2fa-vpn.git

# Проверьте remote
git remote -v

# Загрузите код на GitHub
git push -u origin main
```

### Шаг 5: Аутентификация на GitHub

⚠️ **ВАЖНО**: GitHub больше не принимает обычные пароли! Нужен Personal Access Token.

**Подробная инструкция**: См. файл [`GITHUB_AUTH.md`](GITHUB_AUTH.md) - там все подробно описано!

**Кратко:**
1. Создайте Personal Access Token на GitHub:
   - GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
   - Generate new token (classic)
   - Выберите права: `repo` (все подрепозитории)
   - Скопируйте токен (выглядит как `ghp_xxxxxxxx...`)
2. При `git push` используйте:
   - **Username**: ваш GitHub логин
   - **Password**: ваш Personal Access Token (не пароль!)

**Или используйте SSH** - см. `GITHUB_AUTH.md` для инструкции.

## 🎯 После публикации

### 1. Обновите ссылки в README.md

Найдите и замените в `README.md`:
```bash
# Найдите эту строку:
https://github.com/ваш-username/mikrotik-2fa-vpn.git

# Замените на ваш реальный URL:
https://github.com/YOUR_USERNAME/mikrotik-2fa-vpn.git
```

### 2. Добавьте теги (Topics) на GitHub

На странице репозитория нажмите на шестеренку рядом с "About" и добавьте:
- `mikrotik`
- `vpn`
- `2fa`
- `two-factor-authentication`
- `telegram-bot`
- `fastapi`
- `react`
- `python`
- `router`
- `network-security`

### 3. Настройте описание репозитория

На странице репозитория:
- Добавьте описание: "Система двухфакторной аутентификации для VPN подключений на роутерах MikroTik"
- Добавьте веб-сайт (если есть)
- Добавьте README.md в описание (опционально)

### 4. Создайте первый Release (опционально)

1. Releases → Create a new release
2. Tag: `v1.0.0`
3. Title: `MikroTik 2FA VPN System v1.0.0`
4. Description: Скопируйте из `FINAL_SUMMARY.md`
5. Publish release

## ⚠️ Важные напоминания

### НИКОГДА не коммитьте:
- ❌ `.env` файлы (они уже в `.gitignore`)
- ❌ Базы данных (`*.db` файлы)
- ❌ Логи (`*.log` файлы)
- ❌ Секретные ключи, пароли, токены
- ❌ SSH ключи (`*.key`, `*.pem`, `*.crt`)

### Всегда проверяйте перед коммитом:
```bash
git status  # Проверьте список файлов
git diff    # Проверьте изменения
```

## 📚 Дополнительные ресурсы

- **🚀 Автоматическая настройка**: `./setup_github.sh` - скрипт для автоматической настройки
- **🔑 Аутентификация GitHub**: `GITHUB_AUTH.md` - **ПРОЧТИТЕ ЭТОТ ФАЙЛ!** Подробная инструкция по настройке Personal Access Token
- **Подробная инструкция**: `GITHUB_SETUP.md` (на английском)
- **Руководство для контрибьюторов**: `CONTRIBUTING.md`
- **Документация проекта**: `docs/` папка

## 🔍 Проверка перед публикацией

Выполните эти команды, чтобы убедиться, что все готово:

```bash
cd /root/mikrotik-2fa-vpn

# 1. Проверьте, что нет секретов
find . -name ".env" -not -path "./.env.example"
# Должно быть пусто

# 2. Проверьте, что нет баз данных
find . -name "*.db"
# Должно быть пусто

# 3. Проверьте gitignore
cat .gitignore | grep -E "(\.env|\.db|\.log|node_modules|__pycache__)"
# Должны быть все эти паттерны

# 4. Проверьте статус git
git status
# Все файлы должны быть готовы к коммиту
```

## ✅ Чеклист готовности

- [ ] Git установлен и настроен
- [ ] Репозиторий создан на GitHub
- [ ] `.gitignore` проверен (все секреты исключены)
- [ ] `README.md` обновлен (ссылки на GitHub)
- [ ] `LICENSE` добавлен
- [ ] `CONTRIBUTING.md` добавлен
- [ ] `.env.example` существует
- [ ] Нет файлов с секретами
- [ ] Код загружен на GitHub
- [ ] Теги (Topics) добавлены
- [ ] Описание репозитория заполнено

## 🎉 Готово!

После выполнения всех шагов ваш проект будет на GitHub и готов к использованию!

Если возникнут вопросы или проблемы, проверьте:
- `GITHUB_SETUP.md` - подробная инструкция
- [GitHub Documentation](https://docs.github.com/)
- [Git Handbook](https://guides.github.com/introduction/git-handbook/)

---

**Удачи с публикацией проекта!** 🚀
