# Как использовать SSH для git push

## ✅ Ваш SSH уже настроен!

SSH подключение к GitHub работает успешно:
```
Hi sh034! You've successfully authenticated
```

## 🔄 Переключение remote на SSH

Ваш remote был настроен на HTTPS. Теперь он переключен на SSH.

Проверьте:
```bash
cd /root/mikrotik-2fa-vpn
git remote -v
```

Должно быть:
```
origin  git@github.com:sh034/2FA-Mikrotik-VPN.git (fetch)
origin  git@github.com:sh034/2FA-Mikrotik-VPN.git (push)
```

Если видите `https://github.com/...` - значит remote еще на HTTPS. Измените:
```bash
git remote set-url origin git@github.com:sh034/2FA-Mikrotik-VPN.git
```

## 🚀 Использование git push с SSH

Теперь при выполнении `git push` будет использоваться SSH автоматически:

```bash
cd /root/mikrotik-2fa-vpn

# Загрузить код на GitHub (используется SSH автоматически)
git push -u origin main
```

**Пароль НЕ запрашивается!** SSH работает автоматически.

## 📝 Обычный workflow с SSH

### Добавление изменений:

```bash
# 1. Посмотреть изменения
git status

# 2. Добавить файлы
git add .
# или конкретный файл:
# git add файл.txt

# 3. Создать коммит
git commit -m "Описание изменений"

# 4. Загрузить на GitHub (SSH работает автоматически!)
git push
```

### Первый push (создание ветки на GitHub):

```bash
git push -u origin main
```

Флаг `-u` устанавливает связь между локальной и удаленной веткой, после этого можно просто `git push`.

### Последующие push:

```bash
# Просто push (без -u, так как связь уже установлена)
git push
```

## ✅ Проверка работы SSH

### Проверка подключения:
```bash
ssh -T git@github.com
```

Должно показать:
```
Hi sh034! You've successfully authenticated, but GitHub does not provide shell access.
```

### Проверка remote URL:
```bash
git remote -v
```

Должно быть `git@github.com:...`, а не `https://github.com/...`

### Тестовый push:
```bash
# Создайте небольшое изменение
echo "# Тест SSH" >> TEST.md
git add TEST.md
git commit -m "test: проверка SSH push"
git push
```

Если push прошел без запроса пароля - всё работает! ✅

## 🔄 Если нужно переключиться обратно на HTTPS

(Обычно не нужно, но на случай если нужно):

```bash
git remote set-url origin https://github.com/sh034/2FA-Mikrotik-VPN.git
```

## 📚 Полезные команды

```bash
# Посмотреть текущий remote
git remote -v

# Изменить remote на SSH
git remote set-url origin git@github.com:sh034/2FA-Mikrotik-VPN.git

# Изменить remote на HTTPS
git remote set-url origin https://github.com/sh034/2FA-Mikrotik-VPN.git

# Проверить SSH подключение
ssh -T git@github.com

# Посмотреть статус репозитория
git status

# Посмотреть историю коммитов
git log --oneline

# Посмотреть какие файлы изменены
git status
git diff

# Добавить все изменения
git add .

# Закоммитить
git commit -m "Описание изменений"

# Загрузить на GitHub
git push
```

## ❌ Устранение проблем

### Проблема: "Permission denied (publickey)"

**Решение:**
```bash
# Проверьте SSH ключ
ssh -T git@github.com

# Если не работает, добавьте ключ в ssh-agent
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519

# Проверьте снова
ssh -T git@github.com
```

### Проблема: Git все равно просит пароль

**Причина:** Remote настроен на HTTPS вместо SSH

**Решение:**
```bash
# Проверьте текущий URL
git remote -v

# Если видите https:// - переключите на SSH:
git remote set-url origin git@github.com:sh034/2FA-Mikrotik-VPN.git

# Проверьте снова
git remote -v
```

### Проблема: "fatal: The current branch main has no upstream branch"

**Решение:**
```bash
# При первом push используйте флаг -u
git push -u origin main

# Или установите upstream для текущей ветки
git branch --set-upstream-to=origin/main main
git push
```

---

**Готово! Теперь `git push` будет использовать SSH автоматически, без запроса пароля!** 🚀
